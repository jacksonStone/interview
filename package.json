{
  "name": "test",
  "version": "1.0.0",
  "description": "for skuid",
  "main": "test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jackson Stone",
  "license": "ISC",
  "dependencies": {
    "fs-promise": "^0.5.0"
  }
}
